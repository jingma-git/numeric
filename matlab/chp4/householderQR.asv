function [Q, R] = householderQR(A)
[m, n] = size(A);

Q = zeros(m, m);
R = A;
for j=1:n
    x = R(j:m, j);
    w = zeros(m-j,1);
    w(1, 1) = norm(x);
    v = w - x;
    H = ones(m-j+1) - 2 * (v * v')/(v'*v);
    R = H * A(j:m, j:n);
end